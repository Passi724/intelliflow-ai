APP_QDRANT_URL=http://localhost:6333
APP_QDRANT_COLLECTION=intelliflow-documents

# Required for POST /api/chat (RAG answer generation). Get a key from
# https://console.anthropic.com/ and put it in ai-service/.env (gitignored) —
# do not commit a real key.
ANTHROPIC_API_KEY=
